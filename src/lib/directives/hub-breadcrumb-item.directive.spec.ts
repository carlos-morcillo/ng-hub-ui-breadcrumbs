import { HubItemTemplateDirective } from './hub-item-template.directive';

describe('HubItemTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new HubItemTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
