import { HubBreadcrumbItemDirective } from './breadcrumb-item.directive';

describe('HubItemTemplateDirective', () => {
	it('should create an instance', () => {
		const directive = new HubBreadcrumbItemDirective();
		expect(directive).toBeTruthy();
	});
});
